# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Users routes
GET        /api/users/:id      @controllers.UserController.read(id: Long)
POST       /api/users          @controllers.UserController.create
