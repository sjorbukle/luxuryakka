define(['angular'], function(angular) {
    angular.module('luxuryakka.organizationStructure', ['luxuryakka.organizationStructureBackend']);

    angular.module('luxuryakka.organizationStructureBackend', []);
});